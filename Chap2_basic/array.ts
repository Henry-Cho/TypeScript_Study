{
    // What if setting an array as readonly?

    // function printNames(names : readonly string[]) {
    //     names.push // error message pops up
    // }

    // Tuple?

    let student : [string, number];
    //student = ['string', 1];

   // const [name, num] = student;
}